220802
1. String은 클래스다
 자바와 같은 객체지향프로그래밍 (파이썬,C++, JS)에서는 
 클래스도 데이터 타입(int,double..)이다.

2. String 클래스이기 때문에 String을 비교할 때는 
 == (x) equals (o)

3. boolean형 타입 변수이름은 앞에 is 혹은 has로 시작한다.
 ex) boolean isLogin = true;
	if(ifLogin == true) (x) if(isLogin) (o)

4. if else를 사용할 때 되도록이면 else는 사용하지 말자!
 else를 사용하는 것 보다 if 2개를 사용하는게 더 보기 좋다.

5. for문 반복문 종류(*for, while, do-while, *for-each) 중 하나

6. for문 순서가 있다.
 1.초기식
 2.조건식
 3.작업문
 4.반복 후 작업
for(1.초기문; 2.조건식; 4.반복 후 작업){
	3. ..작업문..
   }
220803
1. 배열을 사용하는 이유 :
변수 선언 낭비 방지

2. 배열 문법 * [] <- 배열을 사용하겠다.
 방법 1. int array[] = {};
 방법 2. int[] array = {};

3. 배열은 0번째 부터 시작 
 int array[] = {3(0번째 원소), 6(1번째 원소)};
 array[0];

4. 배열 안에 원소가 많아지므로 for문을 사용
 for문 배열은 친구사이 

메소드 규칙 (협업을 하기 때문에)
1. 패키지 이름은 소문자
2. 클래스 앞에 대문자로 시작 
3. 메소드 이름은 동사 (run, eat..) 
4. 변수 이름은 명사
5. bool 타입은 앞에 is 혹은 has
6. 상수값은 변수 이름을 대문자로 한다
 ex) double Pi = 3.14;
 ex) String CLASS_NAME = "202"; 